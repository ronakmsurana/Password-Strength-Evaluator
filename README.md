# Password-Strength-Evaluator
A password strength evaluator using stack and trie

# To execute run the below command

```gcc main.c evaluator.c history.c patterns.c suggestions.c visualizer.c crypt.c -o password_evaluator```

# Followed by this command

```./password_evaluator```
